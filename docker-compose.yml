services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ict-trading-academy:latest
    container_name: ict-trading-academy
    ports:
      - "8080:80"
    restart: unless-stopped
